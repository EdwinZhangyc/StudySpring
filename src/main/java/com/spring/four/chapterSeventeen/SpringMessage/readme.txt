第十七章 Spring消息
    本章内容：
        1、异步消息简介
        2、基于JMS的消息功能
        3、使用Spring的AMQP发送消息
        4、消息驱动的POJO
    17.1 异步消息简介
        17.1.1 发送消息
            尽管不同的消息系统会提供不同的消息路由模式，但是有两种通用的目的地：
                1、队列（queue）：点对点模型。
                2、主题（topic）：发布/订阅模型。
            点对点消息模型
            发布-订阅消息模式
        17.1.2 评估异步消息的优点
            无需等待
            面向消息和解耦
                与同步RPC通信相比较，由于客户端与服务器耦合在一起，如服务端有改动，客户端也需要随之调整。
            位置独立
            确保投递
               与同步RPC通信相比较，如果服务端系统崩溃，表示客户端也使用不了。
    17.2 使用JMS发送消息
        17.2.1 在Spring中搭建消息代理
            创建连接工厂
            声明ActiveMQ消息目的地
                目的地可以是一个队列也可以是一个主题，这取决于应用的需求。
        17.2.2 使用Spring的JMS模版
            处理失控的JMS代码
            使用JMS模版
            发送消息
            设置默认目的地
            在发送时，对消息进行转换（convert）
            接受消息
        17.2.3 创建消息驱动的POJO
            由于receive()与receiveAndConvert()方法都是同步接收消息的，因此这些方法会一直在阻塞状态。同步接收异步消息，很奇怪，所以使用消息驱动POJO。
            创建消息监听器
            配置消息监听器（monitor）
        17.2.4 使用基于消息的RPC
            导出基于JMS的服务
            使用基于JMS服务
    17.3 使用AMQP（Advanced Message Queuing Protcol）实现消息功能
